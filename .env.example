# Configuración de entorno para Kronos Web App
# Copia este archivo como .env y ajusta los valores según tu entorno

# Entorno de ejecución
# Valores posibles: development, production, testing
KRONOS_ENV=development

# Configuración de base de datos (opcional)
# Si no se especifica, se detecta automáticamente según el entorno
# DATABASE_PATH=client_calendar_custom.db

# Configuración de desarrollo (opcional)
# DEBUG_MODE=true
# BACKUP_ENABLED=true

# Variables de entorno para detección automática de producción
# Estas son detectadas automáticamente en plataformas de despliegue:
# STREAMLIT_SHARING_MODE (Streamlit Cloud)
# STREAMLIT_CLOUD (Streamlit Cloud)
# DYNO (Heroku)
# RENDER (Render)
# VERCEL (Vercel)
# RAILWAY_ENVIRONMENT (Railway)
