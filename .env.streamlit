# Configuración específica para Streamlit Cloud
# Este archivo puede ser usado como referencia para variables de entorno

# Forzar entorno de producción
KRONOS_ENV=production

# Variables adicionales para asegurar detección de producción
DEPLOYMENT_ENV=production
NODE_ENV=production

# Streamlit Cloud debería detectar automáticamente estas variables:
# STREAMLIT_CLOUD=true (automática)
# STREAMLIT_SERVER_PORT=8501 (automática)
